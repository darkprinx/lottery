version: '3.7'

services:
  lottery-web:
    build: .
    container_name: lottery-container
    env_file:
      - ./.env
    command: /bin/sh -c "./entrypoint.sh"
    volumes:
      - .:/app
    ports:
      - "8081:8081"
    expose:
      - "8081"

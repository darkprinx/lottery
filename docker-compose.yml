version: '3.7'

services:
  lottery-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: lottery-image
    volumes:
      - .:/app
    ports:
      - "8081:8081"
    expose:
      - "8081"
